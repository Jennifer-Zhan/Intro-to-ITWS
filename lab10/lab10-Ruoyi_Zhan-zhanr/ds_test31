1.3
if(h.getStringB()[i]=='.'){
	h.getStringB()[i]=='2';
}

2.1 
0  
1  1
2
3  45 37 109
4
5
6
7  73 31 
8
9


2.2
All the package IDs get the same index value 6 after the self_hash function. It may cause collisions in a hash table and the running time will be very slow.

2.3
In a hash table, instead of using list, we could use binary search tree(or map, or set) to stores the values in that table location, and the worst case would be O(logn) time.